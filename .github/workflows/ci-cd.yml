name: CI/CD Pipeline

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and push Backend image
        run: |
          docker build -f backend/Dockerfile -t mydockerhubuser/backend:latest backend
          docker push mydockerhubuser/backend:latest

      - name: Build and push Frontend image
        run: |
          docker build -f frontend/Dockerfile -t mydockerhubuser/frontend:latest frontend
          docker push mydockerhubuser/frontend:latest
